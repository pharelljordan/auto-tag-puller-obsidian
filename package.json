{
  "name": "auto-tag-puller",
  "version": "1.0.1",
  "scripts": {
    "build": "esbuild main.ts --bundle --external:obsidian --platform=node --target=es2018 --format=cjs --outfile=main.js"
  },
  "dependencies": {
    "obsidian": "^1.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.17.0",
    "typescript": "^4.9.4"
  }
}